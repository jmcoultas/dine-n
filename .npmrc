# Ensure clean installs during deployment
prefer-offline=false
package-lock=true

# Optimize for production deployments
legacy-peer-deps=false
strict-peer-dependencies=false

# Prevent optional dependency failures from blocking installs
optional=true
